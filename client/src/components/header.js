import React, { Component, useEffect, useState } from 'react';

const Header=()=>{
    
    return (
      <div>
        <div class="container">
          <h3>A program that lets you convert digits to any format. HEX, OCTAL, DEC, BIN... and vice versa.</h3>
          <h5>Build with a c++ addon for speed performances.</h5>
         </div> 
      </div>
    );
  }


export default Header;
